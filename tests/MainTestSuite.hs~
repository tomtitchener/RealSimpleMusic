
module Main (
    main
  ) where

import Test.Tasty
import Test.Tasty.QuickCheck
import Test.Tasty.HUnit

import Music.UtilsTest

main :: IO ()
main = defaultMain tests

tests :: TestTree
tests =
  testGroup "Example"
  [
    testSliceInMiddle  "mid slice"        testSliceInMiddle
  , testProperty       "slice all is id"  propSliceAllIsOriginalList
  ]


